---
phase: 03-timeline-feedback
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/projects/[projectId]/tracks/[trackId]/page.tsx
  - src/components/CommentTimeline.tsx
  - src/components/CommentComposer.tsx
  - src/components/VersionList.tsx
autonomous: true

must_haves:
  truths:
    - "User can view a chronological timeline of comments for a track"
    - "User can leave a comment at a chosen timestamp"
  artifacts:
    - path: "src/components/CommentTimeline.tsx"
      provides: "Chronological comment list with timestamps"
    - path: "src/components/CommentComposer.tsx"
      provides: "Timestamped comment form"
    - path: "src/app/projects/[projectId]/tracks/[trackId]/page.tsx"
      provides: "Timeline + composer integration"
  key_links:
    - from: "src/components/CommentComposer.tsx"
      to: "/api/projects/[projectId]/tracks/[trackId]/comments"
      via: "POST"
      pattern: "comments\"\)"
    - from: "src/components/CommentTimeline.tsx"
      to: "/api/projects/[projectId]/tracks/[trackId]/comments"
      via: "GET"
      pattern: "comments\"\)"
---

<objective>
Add a timeline UI and composer for timestamped comments on the track detail page.

Purpose: Provide the core feedback experience anchored to demo playback.
Output: Timeline list + composer wired into track detail view.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/app/projects/[projectId]/tracks/[trackId]/page.tsx
@src/components/VersionList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build comment timeline component</name>
  <files>src/components/CommentTimeline.tsx</files>
  <action>
    - Create a client component that fetches comments for the current track.
    - Render comments in chronological order with timestamp (mm:ss), author label, and body.
    - Provide empty state text when no comments exist.
  </action>
  <verify>`npm run lint`</verify>
  <done>Timeline renders comments in ascending time order with readable timestamps.</done>
</task>

<task type="auto">
  <name>Task 2: Build timestamped comment composer</name>
  <files>src/components/CommentComposer.tsx</files>
  <action>
    - Create a client component with fields: timestampSeconds and body.
    - Allow a user to type timestamp (mm:ss) and convert to seconds for POST payload.
    - On submit, POST to comments API and reset the form.
  </action>
  <verify>`npm run lint`</verify>
  <done>Submitting a comment sends body + timestamp and clears the form.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate timeline + composer into track detail</name>
  <files>src/app/projects/[projectId]/tracks/[trackId]/page.tsx, src/components/VersionList.tsx</files>
  <action>
    - Add a Timeline section below playback and versions.
    - Pass the current trackId + projectId to both components.
    - Ensure version selection (from VersionList) remains independent of comments.
  </action>
  <verify>`npm run lint`</verify>
  <done>Track detail page shows the timeline and composer for the current track.</done>
</task>

</tasks>

<verification>
Manual check: post a comment at 01:10 and see it appear in the timeline.
</verification>

<success_criteria>
Users can read and add timestamped comments on the track detail page.
</success_criteria>

<output>
After completion, create `.planning/phases/03-timeline-feedback/03-02-SUMMARY.md`
</output>
