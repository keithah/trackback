---
phase: 03-timeline-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/app/api/projects/[projectId]/tracks/[trackId]/comments/route.ts
  - src/app/api/projects/[projectId]/tracks/[trackId]/comments/[commentId]/route.ts
autonomous: true

must_haves:
  truths:
    - "User can post a comment at a specific timestamp on a demo"
    - "User can see a chronological list of comments for a track"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Comment model linked to Track and Version"
    - path: "src/app/api/projects/[projectId]/tracks/[trackId]/comments/route.ts"
      provides: "Create/list comments"
      exports: ["GET", "POST"]
  key_links:
    - from: "src/app/api/projects/[projectId]/tracks/[trackId]/comments/route.ts"
      to: "prisma.comment"
      via: "create/findMany"
      pattern: "prisma\\.comment\\.(create|findMany)"
---

<objective>
Add comment data modeling and API endpoints for timestamped feedback on track versions.

Purpose: Enable the core feedback loop before UI integration.
Output: Comment schema + create/list APIs.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add comment model with timestamp</name>
  <files>prisma/schema.prisma</files>
  <action>
    - Add `Comment` model linked to `Track`, `Version`, and `User`.
    - Fields: id, trackId, versionId (optional), userId, body, timestampSeconds, createdAt.
    - Index by trackId + createdAt for chronological listing.
  </action>
  <verify>`npx prisma db push`</verify>
  <done>Schema supports timestamped comments tied to tracks and versions.</done>
</task>

<task type="auto">
  <name>Task 2: Create comment API</name>
  <files>src/app/api/projects/[projectId]/tracks/[trackId]/comments/route.ts</files>
  <action>
    - Implement `GET` to list comments for a track ordered by createdAt ascending.
    - Implement `POST` for payload `{ body, timestampSeconds, versionId? }`.
    - Require project membership; validate timestampSeconds >= 0.
  </action>
  <verify>`npm run lint`</verify>
  <done>Comments can be created and listed for a track.</done>
</task>

<task type="auto">
  <name>Task 3: Add delete endpoint for comment cleanup</name>
  <files>src/app/api/projects/[projectId]/tracks/[trackId]/comments/[commentId]/route.ts</files>
  <action>
    - Implement DELETE to remove a comment if the requester is the author or project owner.
    - Return 403 for unauthorized deletes and 404 for missing comments.
  </action>
  <verify>`npm run lint`</verify>
  <done>Authorized users can delete their own comments (or owners can delete any).</done>
</task>

</tasks>

<verification>
`npx prisma db push` and `npm run lint` pass; POST + GET comments work in API tests.
</verification>

<success_criteria>
Comment data model and APIs support timestamped feedback per track.
</success_criteria>

<output>
After completion, create `.planning/phases/03-timeline-feedback/03-01-SUMMARY.md`
</output>
