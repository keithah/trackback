---
phase: 03-timeline-feedback
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/components/CommentTimeline.tsx
  - src/components/CommentComposer.tsx
autonomous: true

must_haves:
  truths:
    - "New comments appear without a full page refresh"
    - "Timeline updates for all viewers within a short interval"
  artifacts:
    - path: "src/components/CommentTimeline.tsx"
      provides: "Polling or realtime update loop"
    - path: "src/components/CommentComposer.tsx"
      provides: "Optimistic insert or refresh trigger"
  key_links:
    - from: "src/components/CommentTimeline.tsx"
      to: "/api/projects/[projectId]/tracks/[trackId]/comments"
      via: "polling interval"
      pattern: "setInterval"
---

<objective>
Deliver realtime-ish updates for timeline feedback without full refresh.

Purpose: Satisfy realtime requirement with low complexity before websockets.
Output: Comment list that auto-updates and feels live.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/components/CommentTimeline.tsx
@src/components/CommentComposer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add polling to comment timeline</name>
  <files>src/components/CommentTimeline.tsx</files>
  <action>
    - Add a 5-10s polling interval to refresh comments.
    - Deduplicate comments by id when merging.
    - Show a subtle "Updated" timestamp when new comments arrive.
  </action>
  <verify>`npm run lint`</verify>
  <done>Timeline refreshes automatically without a full page reload.</done>
</task>

<task type="auto">
  <name>Task 2: Trigger timeline refresh after posting</name>
  <files>src/components/CommentComposer.tsx</files>
  <action>
    - Add a callback prop `onCommentPosted` to signal the timeline to refresh immediately.
    - Trigger a manual fetch or optimistic insert in CommentTimeline.
  </action>
  <verify>`npm run lint`</verify>
  <done>New comments appear immediately in the timeline after posting.</done>
</task>

</tasks>

<verification>
Manual check: open two tabs and see new comments appear within polling interval.
</verification>

<success_criteria>
Comments update without refresh and appear quickly for all viewers.
</success_criteria>

<output>
After completion, create `.planning/phases/03-timeline-feedback/03-03-SUMMARY.md`
</output>
