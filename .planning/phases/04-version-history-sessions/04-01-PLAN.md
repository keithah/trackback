---
phase: 04-version-history-sessions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/app/api/projects/[projectId]/tracks/[trackId]/versions/route.ts
  - src/app/api/projects/[projectId]/tracks/[trackId]/versions/[versionId]/route.ts
autonomous: true

must_haves:
  truths:
    - "User can see a full version list with names and dates"
    - "User can mark a version as current"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Version fields for current and session markers"
    - path: "src/app/api/projects/[projectId]/tracks/[trackId]/versions/[versionId]/route.ts"
      provides: "Set current version + rollback"
      exports: ["PATCH"]
  key_links:
    - from: "src/app/api/projects/[projectId]/tracks/[trackId]/versions/[versionId]/route.ts"
      to: "prisma.version"
      via: "updateMany/transaction"
      pattern: "prisma\\.version\\.(update|updateMany)"
---

<objective>
Add version history schema enhancements and API support for selecting the current version.

Purpose: Enable version comparison and rollback in the UI.
Output: Version list metadata and current-version API.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Version model for current + session markers</name>
  <files>prisma/schema.prisma</files>
  <action>
    - Add `isCurrent` boolean on Version (default false).
    - Add optional `sessionLabel` and `sessionCreatedAt` for milestone tracking.
    - Ensure existing versions migrate without required fields.
  </action>
  <verify>`npx prisma db push`</verify>
  <done>Schema can represent current version and session markers.</done>
</task>

<task type="auto">
  <name>Task 2: Add API to set current version</name>
  <files>src/app/api/projects/[projectId]/tracks/[trackId]/versions/[versionId]/route.ts</files>
  <action>
    - Implement PATCH to set a version as current for its track.
    - Require project membership; allow only owners to change current.
    - Clear previous current version via transaction.
  </action>
  <verify>`npm run lint`</verify>
  <done>Only owners can mark a version as current; exactly one current version per track.</done>
</task>

<task type="auto">
  <name>Task 3: Expand version list payload</name>
  <files>src/app/api/projects/[projectId]/tracks/[trackId]/versions/route.ts</files>
  <action>
    - Include `isCurrent`, `sessionLabel`, `sessionCreatedAt` in version list response.
  </action>
  <verify>`npm run lint`</verify>
  <done>Version list API returns current + session markers for UI display.</done>
</task>

</tasks>

<verification>
`npx prisma db push` and `npm run lint` pass; PATCH sets current version in DB.
</verification>

<success_criteria>
Current version can be selected and reflected in version lists.
</success_criteria>

<output>
After completion, create `.planning/phases/04-version-history-sessions/04-01-SUMMARY.md`
</output>
