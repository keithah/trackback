---
phase: 04-version-history-sessions
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/VersionList.tsx
  - src/components/VersionCompare.tsx
  - src/app/projects/[projectId]/tracks/[trackId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can view a clear version list with current marker"
    - "User can compare two versions with synced playback"
  artifacts:
    - path: "src/components/VersionList.tsx"
      provides: "Current marker + version select"
    - path: "src/components/VersionCompare.tsx"
      provides: "Side-by-side waveform playback"
  key_links:
    - from: "src/components/VersionCompare.tsx"
      to: "wavesurfer.js"
      via: "two player instances"
      pattern: "WaveSurfer"
---

<objective>
Enable version history display and side-by-side comparison in the track UI.

Purpose: Support analysis and rollback decisions with clear comparison tools.
Output: Version list with current marker and comparison playback UI.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/components/VersionList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add current marker + selection in version list</name>
  <files>src/components/VersionList.tsx</files>
  <action>
    - Show an "Current" badge when `isCurrent` is true.
    - Add selection UI for comparison (checkbox or select two versions).
  </action>
  <verify>`npm run lint`</verify>
  <done>Users can see the current version and pick two versions to compare.</done>
</task>

<task type="auto">
  <name>Task 2: Add version compare player</name>
  <files>src/components/VersionCompare.tsx</files>
  <action>
    - Build a client component that accepts two versions with audio URLs.
    - Render two WaveSurfer players with synced play/pause and seek.
    - Show metadata labels for each version.
  </action>
  <verify>`npm run lint`</verify>
  <done>Users can compare two versions with synced playback.</done>
</task>

<task type="auto">
  <name>Task 3: Wire compare UI into track detail</name>
  <files>src/app/projects/[projectId]/tracks/[trackId]/page.tsx</files>
  <action>
    - Render VersionCompare when two versions are selected.
    - Place compare section near VersionList.
  </action>
  <verify>`npm run lint`</verify>
  <done>Track detail shows comparison panel when versions are selected.</done>
</task>

</tasks>

<verification>
Manual check: select two versions and confirm synced play/pause.
</verification>

<success_criteria>
Version list highlights current version and allows side-by-side playback.
</success_criteria>

<output>
After completion, create `.planning/phases/04-version-history-sessions/04-02-SUMMARY.md`
</output>
