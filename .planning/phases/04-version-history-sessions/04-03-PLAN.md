---
phase: 04-version-history-sessions
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/app/api/projects/[projectId]/tracks/[trackId]/sessions/route.ts
  - src/components/SessionMilestones.tsx
  - src/app/projects/[projectId]/tracks/[trackId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can save a session milestone"
    - "User can see session milestones in history"
  artifacts:
    - path: "src/app/api/projects/[projectId]/tracks/[trackId]/sessions/route.ts"
      provides: "Create/list session milestones"
    - path: "src/components/SessionMilestones.tsx"
      provides: "Milestone list UI"
  key_links:
    - from: "src/components/SessionMilestones.tsx"
      to: "/api/projects/[projectId]/tracks/[trackId]/sessions"
      via: "GET/POST"
      pattern: "sessions\"\)"
---

<objective>
Add session milestone capture and display within track history.

Purpose: Provide a durable record of important sessions in the version history flow.
Output: Session API + milestone list UI.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/app/projects/[projectId]/tracks/[trackId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sessions API</name>
  <files>src/app/api/projects/[projectId]/tracks/[trackId]/sessions/route.ts</files>
  <action>
    - Implement GET to list versions with sessionLabel/sessionCreatedAt.
    - Implement POST to mark the current version as a session milestone with a label.
    - Require project membership; allow only owners to create milestones.
  </action>
  <verify>`npm run lint`</verify>
  <done>Session milestones can be created and listed via API.</done>
</task>

<task type="auto">
  <name>Task 2: Add session milestone UI</name>
  <files>src/components/SessionMilestones.tsx, src/app/projects/[projectId]/tracks/[trackId]/page.tsx</files>
  <action>
    - Create a component that lists milestones with label + date.
    - Add a small form to create a milestone label.
    - Wire to sessions API and refresh on submit.
  </action>
  <verify>`npm run lint`</verify>
  <done>Users can save and view session milestones on the track page.</done>
</task>

</tasks>

<verification>
Manual check: save a milestone and see it appear in the list.
</verification>

<success_criteria>
Session milestones are captured and visible in track history.
</success_criteria>

<output>
After completion, create `.planning/phases/04-version-history-sessions/04-03-SUMMARY.md`
</output>
