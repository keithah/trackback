---
phase: 06-hosting-media-storage
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - deploy/Dockerfile
  - deploy/docker-compose.yml
  - deploy/README.md
autonomous: false

must_haves:
  truths:
    - "App can be deployed reproducibly"
    - "Environment variables and database are documented"
  artifacts:
    - path: "deploy/docker-compose.yml"
      provides: "Production deployment with app + Postgres"
    - path: "deploy/README.md"
      provides: "Deployment instructions"
  key_links:
    - from: "deploy/docker-compose.yml"
      to: "DATABASE_URL"
      via: "env"
      pattern: "DATABASE_URL"
---

<objective>
Provide a self-hosted deployment path using Docker Compose.

Purpose: Enable low-cost hosting on a VPS with reproducible setup.
Output: Docker files and deployment instructions.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dockerfile + Compose config</name>
  <files>deploy/Dockerfile, deploy/docker-compose.yml</files>
  <action>
    - Create production Dockerfile for Next.js app.
    - Compose file runs app + Postgres with persisted volumes.
    - Include env vars for Auth and B2.
  </action>
  <verify>`docker compose -f deploy/docker-compose.yml config`</verify>
  <done>Compose config validates and includes required services.</done>
</task>

<task type="auto">
  <name>Task 2: Document deployment steps</name>
  <files>deploy/README.md</files>
  <action>
    - Document required env vars and DB migration steps.
    - Provide sample commands for build/run/update.
  </action>
  <verify>README includes env var list and steps</verify>
  <done>Deployment instructions are complete and reproducible.</done>
</task>

</tasks>

<verification>
Manual check: `docker compose up` starts app and database.
</verification>

<success_criteria>
Self-host deploy path is documented and works with Compose.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hosting-media-storage/06-02-SUMMARY.md`
</output>
