---
phase: 06-hosting-media-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/storage/b2.ts
  - src/app/api/uploads/route.ts
  - src/app/api/projects/[projectId]/tracks/[trackId]/versions/upload/route.ts
  - prisma/schema.prisma
autonomous: false

user_setup:
  - service: backblaze-b2
    why: "Store uploaded audio"
    env_vars:
      - name: B2_KEY_ID
        source: "Backblaze B2 dashboard -> App Keys"
      - name: B2_APPLICATION_KEY
        source: "Backblaze B2 dashboard -> App Keys"
      - name: B2_BUCKET_ID
        source: "Backblaze B2 dashboard -> Buckets"
      - name: B2_BUCKET_NAME
        source: "Backblaze B2 dashboard -> Buckets"

must_haves:
  truths:
    - "Uploads store audio files in Backblaze B2"
    - "Uploaded audio can be retrieved via signed URLs"
  artifacts:
    - path: "src/lib/storage/b2.ts"
      provides: "B2 upload + signed URL helpers"
    - path: "src/app/api/projects/[projectId]/tracks/[trackId]/versions/upload/route.ts"
      provides: "Upload flow using B2 storage"
  key_links:
    - from: "src/app/api/projects/[projectId]/tracks/[trackId]/versions/upload/route.ts"
      to: "src/lib/storage/b2.ts"
      via: "upload function"
      pattern: "uploadToB2"
---

<objective>
Move uploaded audio storage to Backblaze B2 with signed URL retrieval.

Purpose: Provide production-ready storage without relying on local filesystem.
Output: B2 storage helpers and upload route integration.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/app/api/projects/[projectId]/tracks/[trackId]/versions/upload/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add B2 storage helper</name>
  <files>src/lib/storage/b2.ts</files>
  <action>
    - Implement helper to upload files to B2 using S3-compatible API.
    - Implement helper to generate signed GET URLs for stored objects.
    - Normalize object keys as `projectId/trackId/versionId/filename`.
  </action>
  <verify>`npm run lint`</verify>
  <done>B2 helper can upload and return signed URL metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Update upload route to use B2</name>
  <files>src/app/api/projects/[projectId]/tracks/[trackId]/versions/upload/route.ts, prisma/schema.prisma</files>
  <action>
    - Replace local file writes with B2 upload helper.
    - Persist B2 object key in Version (use `audioPath` or new `audioKey`).
    - Store signed URL in `audioUrl` for playback.
  </action>
  <verify>`npm run lint`</verify>
  <done>Uploads persist to B2 and Versions reference signed URLs.</done>
</task>

</tasks>

<verification>
Manual check: upload file and confirm it appears in B2 with a working signed URL.
</verification>

<success_criteria>
Audio uploads use B2 storage with working signed URLs.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hosting-media-storage/06-01-SUMMARY.md`
</output>
