---
phase: 05-chat-notes
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/app/api/projects/[projectId]/notes/route.ts
  - src/components/ProductionNotesPanel.tsx
  - src/app/projects/[projectId]/tracks/[trackId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can generate production notes from chat"
    - "User can edit and save notes to a track"
  artifacts:
    - path: "src/app/api/projects/[projectId]/notes/route.ts"
      provides: "Generate notes from chat"
    - path: "src/components/ProductionNotesPanel.tsx"
      provides: "Editable notes UI"
  key_links:
    - from: "src/components/ProductionNotesPanel.tsx"
      to: "/api/projects/[projectId]/notes"
      via: "POST"
      pattern: "notes\"\)"
---

<objective>
Add production notes generation and editing tied to a track.

Purpose: Turn chat history into actionable notes for a version.
Output: Notes generator API and editable UI panel.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/app/projects/[projectId]/tracks/[trackId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add notes generation API</name>
  <files>src/app/api/projects/[projectId]/notes/route.ts</files>
  <action>
    - Implement a POST endpoint that summarizes recent chat messages into notes.
    - For now, use a deterministic placeholder summary (no AI integration yet).
    - Require project membership and accept `{ trackId, versionId? }`.
  </action>
  <verify>`npm run lint`</verify>
  <done>API returns a generated notes string for a project/track.</done>
</task>

<task type="auto">
  <name>Task 2: Add production notes panel</name>
  <files>src/components/ProductionNotesPanel.tsx, src/app/projects/[projectId]/tracks/[trackId]/page.tsx</files>
  <action>
    - Create a panel with a "Generate notes" button and editable textarea.
    - On generate, call the notes API and populate the textarea.
    - Add a Save button that stores notes on the track or version (choose track-level note field).
  </action>
  <verify>`npm run lint`</verify>
  <done>Users can generate, edit, and save production notes on a track.</done>
</task>

</tasks>

<verification>
Manual check: generate notes and edit/save them on the track page.
</verification>

<success_criteria>
Production notes workflow exists without external AI dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/05-chat-notes/05-03-SUMMARY.md`
</output>
