---
phase: 05-chat-notes
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/projects/[projectId]/page.tsx
  - src/components/ProjectChatPanel.tsx
autonomous: true

must_haves:
  truths:
    - "User can view project chat history"
    - "User can post messages in the UI"
  artifacts:
    - path: "src/components/ProjectChatPanel.tsx"
      provides: "Chat UI with list + composer"
    - path: "src/app/projects/[projectId]/page.tsx"
      provides: "Chat panel integration"
  key_links:
    - from: "src/components/ProjectChatPanel.tsx"
      to: "/api/projects/[projectId]/chat"
      via: "GET/POST"
      pattern: "chat\"\)"
---

<objective>
Integrate chat UI into the project view with message list and composer.

Purpose: Deliver usable chat flow for collaborators.
Output: Chat panel embedded on project page.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/app/projects/[projectId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build chat panel component</name>
  <files>src/components/ProjectChatPanel.tsx</files>
  <action>
    - Create a client component that fetches chat history on mount.
    - Render messages in chronological order with author + timestamp.
    - Provide a composer input that posts to the chat API.
  </action>
  <verify>`npm run lint`</verify>
  <done>Chat panel shows history and can send new messages.</done>
</task>

<task type="auto">
  <name>Task 2: Embed chat panel in project view</name>
  <files>src/app/projects/[projectId]/page.tsx</files>
  <action>
    - Add a chat section to the project page below the track list.
    - Pass projectId into the chat component.
  </action>
  <verify>`npm run lint`</verify>
  <done>Project page includes chat panel for the current project.</done>
</task>

</tasks>

<verification>
Manual check: post a message and see it appear in the list.
</verification>

<success_criteria>
Users can read and send chat messages in the project UI.
</success_criteria>

<output>
After completion, create `.planning/phases/05-chat-notes/05-02-SUMMARY.md`
</output>
