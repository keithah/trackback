---
phase: 05-chat-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/app/api/projects/[projectId]/chat/route.ts
autonomous: true

must_haves:
  truths:
    - "User can post messages in a project chat thread"
    - "User can view project chat history"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "ChatMessage model linked to Project and User"
    - path: "src/app/api/projects/[projectId]/chat/route.ts"
      provides: "Chat create/list endpoints"
      exports: ["GET", "POST"]
  key_links:
    - from: "src/app/api/projects/[projectId]/chat/route.ts"
      to: "prisma.chatMessage"
      via: "create/findMany"
      pattern: "prisma\\.chatMessage\\.(create|findMany)"
---

<objective>
Add chat data modeling and API endpoints for project-level messaging.

Purpose: Establish the communication channel before UI integration.
Output: ChatMessage schema and basic GET/POST API.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add chat message model</name>
  <files>prisma/schema.prisma</files>
  <action>
    - Add `ChatMessage` model linked to `Project` and `User`.
    - Fields: id, projectId, userId, body, createdAt.
    - Index by projectId + createdAt for chronological listing.
  </action>
  <verify>`npx prisma db push`</verify>
  <done>Schema supports project chat messages tied to users.</done>
</task>

<task type="auto">
  <name>Task 2: Add chat API endpoints</name>
  <files>src/app/api/projects/[projectId]/chat/route.ts</files>
  <action>
    - Implement `GET` to list chat messages for a project ordered by createdAt ascending.
    - Implement `POST` for payload `{ body }`.
    - Require project membership for both operations.
  </action>
  <verify>`npm run lint`</verify>
  <done>Chat messages can be created and listed via API.</done>
</task>

</tasks>

<verification>
`npx prisma db push` and `npm run lint` pass; chat API responds with messages.
</verification>

<success_criteria>
Project chat APIs are available for UI integration.
</success_criteria>

<output>
After completion, create `.planning/phases/05-chat-notes/05-01-SUMMARY.md`
</output>
