---
phase: 02-media-ingest-playback
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/projects/[projectId]/tracks/[trackId]/page.tsx
  - src/components/VersionUploadPanel.tsx
  - src/components/VersionList.tsx
autonomous: true

must_haves:
  truths:
    - "User can upload a demo with visible progress"
    - "User can add an external audio link as a demo"
    - "Uploaded demos show up in the track detail list"
  artifacts:
    - path: "src/components/VersionUploadPanel.tsx"
      provides: "Upload + external link UI with progress"
    - path: "src/components/VersionList.tsx"
      provides: "Version listing with metadata"
    - path: "src/app/projects/[projectId]/tracks/[trackId]/page.tsx"
      provides: "Track detail integration"
  key_links:
    - from: "src/components/VersionUploadPanel.tsx"
      to: "/api/projects/[projectId]/tracks/[trackId]/versions/upload"
      via: "multipart upload"
      pattern: "versions/upload"
    - from: "src/components/VersionUploadPanel.tsx"
      to: "/api/projects/[projectId]/tracks/[trackId]/versions"
      via: "JSON POST"
      pattern: "versions\"\)"
---

<objective>
Expose upload and external link flows in the track detail UI with real progress feedback and a live version list.

Purpose: Make media ingest usable for collaborators without leaving the track page.
Output: Upload panel + version list integrated into the track detail page.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/app/projects/[projectId]/tracks/[trackId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add version list UI</name>
  <files>src/components/VersionList.tsx, src/app/projects/[projectId]/tracks/[trackId]/page.tsx</files>
  <action>
    - Create `VersionList` component that renders versions with name, createdAt, source, and metadata (duration, sample rate, bitrate).
    - Update track detail query to include versions with media fields.
    - Render `VersionList` under the track header, ordered newest-first.
  </action>
  <verify>`npm run lint`</verify>
  <done>Track detail shows all versions with basic media metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add upload + external link panel with progress</name>
  <files>src/components/VersionUploadPanel.tsx, src/app/projects/[projectId]/tracks/[trackId]/page.tsx</files>
  <action>
    - Build `VersionUploadPanel` as a client component with two modes: Upload and External Link.
    - Use `XMLHttpRequest` for uploads to report progress (`onprogress`).
    - Submit external links via JSON POST.
    - On success, refresh the page to show the new version.
    - Validate file types (wav/aiff/flac) client-side and show inline errors.
  </action>
  <verify>`npm run lint`</verify>
  <done>Users can upload with progress or add a link and see the new version in the list.</done>
</task>

</tasks>

<verification>
Manual check: upload a WAV or add a link; list updates with metadata.
</verification>

<success_criteria>
Track detail page supports upload + external link flows with visible progress and list updates.
</success_criteria>

<output>
After completion, create `.planning/phases/02-media-ingest-playback/02-02-SUMMARY.md`
</output>
