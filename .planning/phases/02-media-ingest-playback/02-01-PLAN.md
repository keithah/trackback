---
phase: 02-media-ingest-playback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/media.ts
  - src/app/api/projects/[projectId]/tracks/[trackId]/versions/route.ts
  - src/app/api/projects/[projectId]/tracks/[trackId]/versions/upload/route.ts
autonomous: true

must_haves:
  truths:
    - "User can upload an audio file for a track version"
    - "User can add an external audio link as a version"
    - "Uploaded audio persists across sessions"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Version media fields (source, urls, metadata)"
    - path: "src/app/api/projects/[projectId]/tracks/[trackId]/versions/route.ts"
      provides: "Create/list versions via JSON"
      exports: ["GET", "POST"]
    - path: "src/app/api/projects/[projectId]/tracks/[trackId]/versions/upload/route.ts"
      provides: "Multipart upload handler"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/projects/[projectId]/tracks/[trackId]/versions/upload/route.ts"
      to: "public/uploads"
      via: "fs write"
      pattern: "fs\\.writeFile"
    - from: "src/app/api/projects/[projectId]/tracks/[trackId]/versions/route.ts"
      to: "prisma.version"
      via: "create/findMany"
      pattern: "prisma\\.version\\.(create|findMany)"
---

<objective>
Introduce version-level media storage and API endpoints for both uploaded audio files and external links.

Purpose: Provide durable media artifacts that later UI and playback can rely on.
Output: Version media fields, upload + link APIs, and file persistence in local storage.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Version model for media metadata</name>
  <files>prisma/schema.prisma</files>
  <action>
    - Add media fields on Version: `audioUrl`, `audioPath`, `audioMime`, `audioDurationSeconds`, `audioSampleRate`, `audioBitrateKbps`, `audioSource`.
    - Use nullable types, with `audioSource` as an enum (UPLOAD | EXTERNAL) to distinguish uploads from links.
    - Keep existing Version creation paths intact (no required fields).
  </action>
  <verify>`npx prisma db push`</verify>
  <done>Version records can store audio metadata without breaking existing versions.</done>
</task>

<task type="auto">
  <name>Task 2: Add media helpers and upload API</name>
  <files>src/lib/media.ts, src/app/api/projects/[projectId]/tracks/[trackId]/versions/upload/route.ts</files>
  <action>
    - Create `src/lib/media.ts` to:
      - Write uploaded files to `public/uploads` with a safe filename.
      - Extract audio metadata using `music-metadata` (duration, sample rate, bitrate, mime).
      - Return `{ url, path, metadata }` for storage.
    - Implement `POST /api/projects/[projectId]/tracks/[trackId]/versions/upload`:
      - Accept `multipart/form-data` with fields: `file`, `name`, `notes`.
      - Require project membership.
      - Create a Version record with `audioSource=UPLOAD` and metadata from `media.ts`.
  </action>
  <verify>`npm run lint`</verify>
  <done>Multipart uploads create a Version with stored file URL and metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Add external link version API + list</name>
  <files>src/app/api/projects/[projectId]/tracks/[trackId]/versions/route.ts</files>
  <action>
    - Implement `GET` to list versions for a track ordered by `createdAt desc` including media fields.
    - Implement `POST` for JSON payload `{ name, notes?, externalUrl }`:
      - Validate `externalUrl` as URL.
      - Require project membership.
      - Create Version with `audioSource=EXTERNAL` and `audioUrl` set.
  </action>
  <verify>`npm run lint`</verify>
  <done>Versions can be created with external URLs and listed with media data.</done>
</task>

</tasks>

<verification>
`npx prisma db push` and `npm run lint` pass; POST upload returns a Version with audio metadata.
</verification>

<success_criteria>
Uploads and external links both create Version records with media data and persisted access URLs.
</success_criteria>

<output>
After completion, create `.planning/phases/02-media-ingest-playback/02-01-SUMMARY.md`
</output>
