---
phase: 02-media-ingest-playback
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/components/AudioPlayer.tsx
  - src/components/VersionList.tsx
  - package.json
autonomous: true

must_haves:
  truths:
    - "User can play uploaded demos in the track view"
    - "User can see a waveform for the selected demo"
    - "User can view extracted audio metadata"
  artifacts:
    - path: "src/components/AudioPlayer.tsx"
      provides: "Waveform playback UI"
    - path: "src/components/VersionList.tsx"
      provides: "Selects a version for playback"
  key_links:
    - from: "src/components/AudioPlayer.tsx"
      to: "wavesurfer.js"
      via: "client init"
      pattern: "WaveSurfer"
---

<objective>
Provide waveform playback and metadata display for track versions.

Purpose: Make demos playable and visually scannable, satisfying media playback requirements.
Output: Audio player with waveform and version selection wiring.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/components/VersionList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add waveform player component</name>
  <files>src/components/AudioPlayer.tsx, package.json</files>
  <action>
    - Add `wavesurfer.js` dependency.
    - Build a client `AudioPlayer` that accepts `audioUrl` and `metadata` props.
    - Render play/pause controls and a waveform canvas.
    - Clean up WaveSurfer instance on unmount.
  </action>
  <verify>`npm run lint`</verify>
  <done>AudioPlayer renders a waveform and plays audio when provided a URL.</done>
</task>

<task type="auto">
  <name>Task 2: Wire version selection to playback</name>
  <files>src/components/VersionList.tsx</files>
  <action>
    - Add selection state for versions.
    - Display metadata (duration, sample rate, bitrate) for the selected version.
    - Render `AudioPlayer` for the selected version and show a placeholder if no audio URL.
  </action>
  <verify>`npm run lint`</verify>
  <done>Users can select a version and play it with waveform + metadata visible.</done>
</task>

</tasks>

<verification>
Manual check: select a version with audio and confirm playback + waveform render.
</verification>

<success_criteria>
Track detail page supports waveform playback and metadata display for uploaded demos.
</success_criteria>

<output>
After completion, create `.planning/phases/02-media-ingest-playback/02-03-SUMMARY.md`
</output>
