---
phase: 01-projects-access
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - src/lib/permissions.ts
  - src/lib/validators/project.ts
  - src/lib/validators/track.ts
  - src/lib/validators/invite.ts
  - src/app/api/projects/route.ts
  - src/app/api/projects/[projectId]/route.ts
  - src/app/api/projects/[projectId]/tracks/route.ts
  - src/app/api/projects/[projectId]/tracks/[trackId]/route.ts
  - src/app/api/projects/[projectId]/invites/route.ts
  - src/app/api/invites/[token]/accept/route.ts
autonomous: true

must_haves:
  truths:
    - "User can create projects and tracks via server APIs"
    - "Track status updates and deletes are enforced server-side"
    - "Collaborator invites can be created by email or username"
  artifacts:
    - path: "src/app/api/projects/route.ts"
      provides: "Project list and create endpoints"
      exports: ["GET", "POST"]
    - path: "src/app/api/projects/[projectId]/tracks/route.ts"
      provides: "Track creation endpoint"
      exports: ["POST"]
    - path: "src/app/api/projects/[projectId]/invites/route.ts"
      provides: "Invite creation endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/projects/[projectId]/tracks/[trackId]/route.ts"
      to: "src/lib/permissions.ts"
      via: "owner check before delete"
      pattern: "requireOwner"
    - from: "src/app/api/projects/[projectId]/invites/route.ts"
      to: "src/lib/validators/invite.ts"
      via: "payload validation"
      pattern: "inviteSchema"
---

<objective>
Implement server APIs for projects, tracks, status updates, and collaborator invites with owner-only deletes.

Purpose: Provide the backend surface area for Phase 1 UI flows and enforce access control rules.
Output: Route handlers and validators for project/track/invite CRUD operations.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-projects-access/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validators and permission helpers</name>
  <files>src/lib/permissions.ts, src/lib/validators/project.ts, src/lib/validators/track.ts, src/lib/validators/invite.ts</files>
  <action>
    - Create Zod schemas for project create, track create/status update, and invite creation.
    - Add permission helpers:
      - `requireProjectMember(userId, projectId)`
      - `requireProjectOwner(userId, projectId)`
    - Use Prisma to check membership and role (OWNER vs COLLABORATOR).
  </action>
  <verify>`npm run lint`</verify>
  <done>Validators and permission helpers are available for API routes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement project + track route handlers</name>
  <files>src/app/api/projects/route.ts, src/app/api/projects/[projectId]/route.ts, src/app/api/projects/[projectId]/tracks/route.ts, src/app/api/projects/[projectId]/tracks/[trackId]/route.ts</files>
  <action>
    - `GET /api/projects`: return projects for the current user ordered by latest activity.
    - `GET /api/projects/[projectId]`: return project details and current user's membership role.
    - `POST /api/projects`: create a project with name, optional description, and defaultTrackStatus (default DEMO); create OWNER membership for the current user.
    - `GET /api/projects/[projectId]/tracks`: return tracks for the project ordered by latest activity.
    - `POST /api/projects/[projectId]/tracks`: create a track with name and optional notes; enforce unique name per project; set status from request or project default.
    - `PATCH /api/projects/[projectId]/tracks/[trackId]`: update track status.
    - `DELETE /api/projects/[projectId]/tracks/[trackId]`: require OWNER role; delete track.
    - Ensure `updatedAt` changes for status updates and track edits so dashboard ordering is correct.
  </action>
  <verify>`npm run lint`</verify>
  <done>Project and track APIs compile and enforce uniqueness and owner-only deletes.</done>
</task>

<task type="auto">
  <name>Task 3: Implement invite creation and acceptance</name>
  <files>src/app/api/projects/[projectId]/invites/route.ts, src/app/api/invites/[token]/accept/route.ts</files>
  <action>
    - `POST /api/projects/[projectId]/invites`: accept email or username.
      - If username matches an existing user, create membership directly (COLLABORATOR) and return success.
      - If email provided (or username not found), create Invite with token bound to target; send email via Resend with acceptance link.
    - `POST /api/invites/[token]/accept`: validate token and target match; create membership and mark invite accepted.
    - Ensure only project members can create invites; only owners can invite if you add owner-only enforcement.
  </action>
  <verify>`npm run lint`</verify>
  <done>Invite APIs compile and enforce token/target validation.</done>
</task>

</tasks>

<verification>
`npm run lint` succeeds after adding all API routes.
</verification>

<success_criteria>
All Phase 1 server endpoints exist with validation and owner-only delete enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/01-projects-access/01-04-SUMMARY.md`
</output>
