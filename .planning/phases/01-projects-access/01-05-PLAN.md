---
phase: 01-projects-access
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/signin/page.tsx
autonomous: true

must_haves:
  truths:
    - "App renders the Trackback visual system shell"
    - "User can reach a sign-in page that triggers Auth.js sign-in"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with fonts and global shell"
    - path: "src/app/globals.css"
      provides: "Global variables and baseline styling"
    - path: "src/app/signin/page.tsx"
      provides: "Sign-in page that calls signIn()"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import"
      pattern: "globals.css"
    - from: "src/app/signin/page.tsx"
      to: "next-auth"
      via: "signIn call"
      pattern: "signIn"
---

<objective>
Create the Phase 1 UI shell and sign-in entry point.

Purpose: Establish the visual system and authentication entry point before building the dashboard flows.
Output: A styled, responsive shell with a working sign-in page.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-projects-access/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a distinct UI shell and sign-in page</name>
  <files>src/app/layout.tsx, src/app/globals.css, src/app/signin/page.tsx</files>
  <action>
    - Set up a clear visual system: define CSS variables for background, surface, text, and accent colors; use a non-default font (avoid Inter/Roboto/Arial).
    - Use `next/font/google` to load a display font (e.g., Space Grotesk) and a body font (e.g., Work Sans).
    - Add a subtle gradient or textured background and card surfaces to avoid flat white UI.
    - Create `/signin` page with a minimal login CTA that calls `signIn()` for the configured Auth.js provider.
  </action>
  <verify>`npm run lint`</verify>
  <done>App renders with the new visual system and a working sign-in page.</done>
</task>

</tasks>

<verification>
`npm run lint` succeeds and the sign-in page renders without runtime errors.
</verification>

<success_criteria>
Visual system and sign-in entry point are ready for the dashboard flows.
</success_criteria>

<output>
After completion, create `.planning/phases/01-projects-access/01-05-SUMMARY.md`
</output>
