---
phase: 01-projects-access
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - .planning/PROJECT.md
  - prisma/schema.prisma
  - src/db/prisma.ts
  - src/auth.ts
  - src/app/api/auth/[...nextauth]/route.ts
  - src/lib/auth.ts
  - .env.example
autonomous: true
user_setup:
  - service: resend
    why: "Send magic-link sign-in emails and collaborator invites"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys"
      - name: RESEND_FROM
        source: "Resend Dashboard -> Domains (verified sender)"
  - service: github-oauth
    why: "Only if option-b (GitHub OAuth) is selected"
    env_vars:
      - name: GITHUB_CLIENT_ID
        source: "GitHub Developer Settings -> OAuth Apps"
      - name: GITHUB_CLIENT_SECRET
        source: "GitHub Developer Settings -> OAuth Apps"
    dashboard_config:
      - task: "Add callback URL"
        location: "GitHub OAuth App -> Authorization callback URL (e.g. http://localhost:3000/api/auth/callback/github)"

must_haves:
  truths:
    - "Project, track, membership, and invite data can be stored with correct constraints"
    - "Server components can access an authenticated session"
    - "Track status defaults to Demo when created"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Project/track/invite models and Auth.js adapter models"
      contains: "model Project"
    - path: "src/auth.ts"
      provides: "Auth.js configuration for chosen auth method"
    - path: "src/db/prisma.ts"
      provides: "Prisma client singleton"
  key_links:
    - from: "src/app/api/auth/[...nextauth]/route.ts"
      to: "src/auth.ts"
      via: "Auth handler import"
      pattern: "auth"
    - from: "src/auth.ts"
      to: "prisma/schema.prisma"
      via: "PrismaAdapter models"
      pattern: "PrismaAdapter"
    - from: "prisma/schema.prisma"
      to: "Track"
      via: "@@unique([projectId, name])"
      pattern: "@@unique\(\[projectId, name\]\)"
---

<objective>
Add the Phase 1 data model and Auth.js configuration so access control and API routes can be built safely.

Purpose: Enforce track-name uniqueness, represent memberships/invites, and enable authenticated sessions for server checks.
Output: Prisma schema, Auth.js setup, and a session helper ready for API and UI use.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-projects-access/01-CONTEXT.md
@.planning/phases/01-projects-access/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the chosen auth method in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <action>
    - Add a new row to the Key Decisions table documenting the selected auth method from Plan 01-01.
    - Include a short rationale and mark the outcome as the chosen option.
  </action>
  <verify>Open `.planning/PROJECT.md` and confirm the decision row is present.</verify>
  <done>The auth method decision is recorded with rationale and outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Define Prisma schema for auth + projects + tracks + invites</name>
  <files>prisma/schema.prisma, src/db/prisma.ts, .env.example</files>
  <action>
    - Initialize Prisma and add models for Auth.js (User, Account, Session, VerificationToken).
    - Add domain models: Project, Membership, Track, Invite.
    - Enforce track name uniqueness per project with `@@unique([projectId, name])`.
    - Add `TrackStatus` enum with values: DEMO, MIXING, MASTERED, RELEASED; default Track.status to DEMO.
    - Add `Project.defaultTrackStatus` (default DEMO) to support project-first creation flow.
    - Add `MembershipRole` enum (OWNER, COLLABORATOR) and ensure only one membership per user per project.
    - Add `Invite` fields: token, projectId, email/username target, invitedById, status, expiresAt; bind acceptance to target.
    - Add `src/db/prisma.ts` Prisma client singleton and update `.env.example` with `DATABASE_URL`.
  </action>
  <verify>`npx prisma validate`</verify>
  <done>Schema validates with required constraints and enums.</done>
</task>

<task type="auto">
  <name>Task 3: Configure Auth.js for the selected login method</name>
  <files>src/auth.ts, src/app/api/auth/[...nextauth]/route.ts, src/lib/auth.ts, .env.example</files>
  <action>
    - Implement Auth.js config in `src/auth.ts` using the option selected in Plan 01-01:
      - If option-a: use Email provider with Resend.
      - If option-b: use GitHub OAuth provider.
    - Use PrismaAdapter for session persistence and set `session: { strategy: "database" }`.
    - Add `src/app/api/auth/[...nextauth]/route.ts` to export GET/POST handlers.
    - Add `src/lib/auth.ts` with `requireSession()` helper that throws on missing session.
    - Update `.env.example` with required Auth.js and provider env vars (`AUTH_SECRET`, `AUTH_URL`, and provider-specific keys).
  </action>
  <verify>`npm run lint`</verify>
  <done>Auth.js handlers compile and `requireSession()` is available for server routes.</done>
</task>

</tasks>

<verification>
`npx prisma validate` and `npm run lint` both succeed.
</verification>

<success_criteria>
Database schema and Auth.js configuration are ready for API routes and UI access checks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-projects-access/01-03-SUMMARY.md`
</output>
