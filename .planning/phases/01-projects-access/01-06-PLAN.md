---
phase: 01-projects-access
plan: 06
type: execute
wave: 5
depends_on: ["01-05"]
files_modified:
  - src/app/page.tsx
  - src/app/projects/[projectId]/page.tsx
  - src/app/projects/[projectId]/tracks/[trackId]/page.tsx
  - src/components/ProjectCard.tsx
  - src/components/ProjectCreateModal.tsx
  - src/components/TrackCard.tsx
  - src/components/TrackCreateModal.tsx
  - src/components/TrackStatusSelect.tsx
  - src/components/InviteModal.tsx
autonomous: true

must_haves:
  truths:
    - "User can create a project and immediately add a track from the empty state"
    - "User can view tracks grouped into Active and Finished sections ordered by recent activity"
    - "User can set and view track status from the UI"
    - "User can invite collaborators by email or username"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Project list and create flow entry point"
    - path: "src/app/projects/[projectId]/page.tsx"
      provides: "Project dashboard with grouped track cards"
    - path: "src/app/projects/[projectId]/tracks/[trackId]/page.tsx"
      provides: "Track detail page with status controls"
    - path: "src/components/TrackCreateModal.tsx"
      provides: "Track creation form (name + notes)"
  key_links:
    - from: "src/components/ProjectCreateModal.tsx"
      to: "/api/projects"
      via: "POST request"
      pattern: "fetch\(.*api/projects"
    - from: "src/components/TrackCreateModal.tsx"
      to: "/api/projects/[projectId]/tracks"
      via: "POST request"
      pattern: "api/projects/.*/tracks"
    - from: "src/components/TrackStatusSelect.tsx"
      to: "/api/projects/[projectId]/tracks/[trackId]"
      via: "PATCH request + state refresh"
      pattern: "PATCH.*api/projects/.*/tracks/.*/"
    - from: "src/components/InviteModal.tsx"
      to: "/api/projects/[projectId]/invites"
      via: "POST request"
      pattern: "api/projects/.*/invites"
---

<objective>
Build the Phase 1 UI flows for project creation, multi-track dashboard, track status, and collaborator invites.

Purpose: Deliver the core workflows users need to create projects, manage tracks, and collaborate.
Output: A responsive UI wired to Phase 1 APIs with the required flows and constraints.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-projects-access/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build project list + create project flow</name>
  <files>src/app/page.tsx, src/components/ProjectCard.tsx, src/components/ProjectCreateModal.tsx</files>
  <action>
    - Use `auth()` in the root page to require a session; redirect to `/signin` if not authenticated.
    - Render a project grid (card layout) showing project name and updated date.
    - Add a create project modal with inputs: name, optional description, default track status (pre-selected to Demo).
    - Wire the modal submit to `POST /api/projects` and use the response `projectId` for the redirect.
    - On success, redirect to `/projects/[projectId]?created=1` so the project dashboard can open the add-track modal immediately.
  </action>
  <verify>`npm run lint`</verify>
  <done>Users can create a project and are routed to the project dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: Build project dashboard, track page, and invite flow</name>
  <files>src/app/projects/[projectId]/page.tsx, src/app/projects/[projectId]/tracks/[trackId]/page.tsx, src/components/TrackCard.tsx, src/components/TrackCreateModal.tsx, src/components/TrackStatusSelect.tsx, src/components/InviteModal.tsx</files>
  <action>
    - Project dashboard:
      - Fetch tracks and split into Active (DEMO, MIXING) and Finished (MASTERED, RELEASED) groups.
      - Order each group by most recent activity (`updatedAt`).
      - Show cards with name, status, and last updated; include “Add Track” and “Invite” actions.
      - If `created=1` query param is present, auto-open the add-track modal.
    - Track creation modal:
      - Collect track name + optional notes; on success, navigate to `/projects/[projectId]/tracks/[trackId]`.
      - Wire the modal submit to `POST /api/projects/[projectId]/tracks` and use the response `trackId` for navigation.
    - Track page:
      - Show track name, notes, and a status selector using `TrackStatusSelect`.
      - `TrackStatusSelect` must `PATCH /api/projects/[projectId]/tracks/[trackId]` and refresh the local track state after success.
      - Provide delete button only for owners (and rely on server enforcement).
    - Invite modal:
      - Accept email or username input; call invite API and show success/error states.
  </action>
  <verify>`npm run lint`</verify>
  <done>Dashboard and track pages support status updates, creation flow, and invites per Phase 1 requirements.</done>
</task>

</tasks>

<verification>
`npm run lint` succeeds, and manual smoke testing confirms project creation → add track → track page navigation.
</verification>

<success_criteria>
Phase 1 UI flows work end-to-end: project creation, track creation, status updates, and collaborator invites.
</success_criteria>

<output>
After completion, create `.planning/phases/01-projects-access/01-06-SUMMARY.md`
</output>
