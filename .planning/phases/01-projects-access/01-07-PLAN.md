---
phase: 01-projects-access
plan: 07
type: execute
wave: 6
depends_on: ["01-06"]
files_modified:
  - prisma/schema.prisma
  - src/app/api/projects/[projectId]/tracks/route.ts
  - src/app/api/projects/[projectId]/tracks/[trackId]/versions/[versionId]/route.ts
  - src/app/projects/[projectId]/page.tsx
  - src/components/TrackCard.tsx
  - src/app/projects/[projectId]/tracks/[trackId]/page.tsx
  - src/components/TrackStatusSelect.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Project owners can delete a track version; collaborators cannot"
    - "Project dashboards show each track's latest version metadata"
    - "Track detail pages show the latest version with a delete control for owners"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Version model linked to Track"
    - path: "src/app/api/projects/[projectId]/tracks/[trackId]/versions/[versionId]/route.ts"
      provides: "Owner-only version deletion"
      exports: ["DELETE"]
    - path: "src/components/TrackCard.tsx"
      provides: "Latest version display in dashboard cards"
    - path: "src/app/projects/[projectId]/tracks/[trackId]/page.tsx"
      provides: "Latest version display and delete control"
  key_links:
    - from: "src/app/api/projects/[projectId]/tracks/route.ts"
      to: "prisma.version"
      via: "transactional create"
      pattern: "prisma\\.version\\.create"
    - from: "src/app/api/projects/[projectId]/tracks/[trackId]/versions/[versionId]/route.ts"
      to: "prisma.version"
      via: "delete"
      pattern: "prisma\\.version\\.delete"
    - from: "src/components/TrackStatusSelect.tsx"
      to: "/api/projects/[projectId]/tracks/[trackId]/versions/[versionId]"
      via: "DELETE request"
      pattern: "api/projects/.*/tracks/.*/versions/.*/"
---

<objective>
Close Phase 1 verification gaps by introducing minimal version data and owner-only version deletion, then surface the latest version in the dashboard and track detail views.

Purpose: Satisfy the owner-only delete requirement and the dashboard "latest version" requirement without expanding into full version history.
Output: Version model + deletion API, plus UI wiring that displays latest version data.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-projects-access/01-CONTEXT.md
@.planning/phases/01-projects-access/01-projects-access-VERIFICATION.md
@.planning/phases/01-projects-access/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add version data model and seed initial versions</name>
  <files>prisma/schema.prisma, src/app/api/projects/[projectId]/tracks/route.ts</files>
  <action>
    - Add a `Version` model linked to `Track` with fields: id, trackId, name, createdAt, updatedAt.
    - Add a `versions` relation on `Track` so Prisma can fetch latest versions.
    - Update track creation (`POST /api/projects/[projectId]/tracks`) to create an initial version inside the same transaction.
    - Use a clear initial version name ("Initial version"), and set timestamps automatically.
  </action>
  <verify>`npx prisma db push`</verify>
  <done>Every new track gets a first Version record linked to it in the database.</done>
</task>

<task type="auto">
  <name>Task 2: Add owner-only version deletion API</name>
  <files>src/app/api/projects/[projectId]/tracks/[trackId]/versions/[versionId]/route.ts</files>
  <action>
    - Create a DELETE route that loads the version and its track/project relation.
    - Enforce owner-only deletion using the existing `requireProjectOwner` helper used by track deletes.
    - Return a 404 if the version or track is missing, and a 403 if the user is not the project owner.
  </action>
  <verify>`npm run lint`</verify>
  <done>DELETE requests remove a version only when the requester is the project owner.</done>
</task>

<task type="auto">
  <name>Task 3: Surface latest version data in dashboard + track detail</name>
  <files>src/app/projects/[projectId]/page.tsx, src/components/TrackCard.tsx, src/app/projects/[projectId]/tracks/[trackId]/page.tsx, src/components/TrackStatusSelect.tsx</files>
  <action>
    - Update the project dashboard query to select each track's latest version (order by createdAt desc, take 1).
    - Extend `TrackCard` to display latest version name and timestamp alongside status.
    - Update the track detail query to include latest version info and render a "Latest version" section.
    - Add a client-side `VersionDeleteButton` (in `TrackStatusSelect.tsx`) that calls the DELETE version API and refreshes the page on success.
    - Render the version delete button only when the viewer is an owner and a latest version exists.
  </action>
  <verify>`npm run lint`</verify>
  <done>Dashboard and track pages show latest version data, and owners can delete versions from the track page.</done>
</task>

</tasks>

<verification>
`npx prisma db push` succeeds, `npm run lint` passes, and deleting a version as a non-owner returns 403.
</verification>

<success_criteria>
Phase 1 gap closed: versions exist, the dashboard shows latest version data, and only owners can delete versions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-projects-access/01-07-SUMMARY.md`
</output>
