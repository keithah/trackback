---
phase: 01-projects-access
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - tailwind.config.ts
  - postcss.config.js
autonomous: true

must_haves:
  truths:
    - "The app boots to a basic Trackback shell without errors"
    - "Tailwind styles compile and apply in the App Router"
  artifacts:
    - path: "package.json"
      provides: "Next.js app scripts and dependencies"
    - path: "src/app/layout.tsx"
      provides: "Root layout for App Router"
    - path: "src/app/globals.css"
      provides: "Global styles and Tailwind base"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import"
      pattern: "globals.css"
---

<objective>
Scaffold the Next.js App Router project with Tailwind so Phase 1 features can be built on a stable foundation.

Purpose: Establish the web app baseline and styling pipeline before adding data models and API routes.
Output: A working Next.js app with Tailwind configured and a minimal Trackback shell.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-projects-access/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js app with Tailwind + TypeScript</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, tailwind.config.ts, postcss.config.js</files>
  <action>
    - Scaffold a Next.js App Router project in the repo root (TypeScript, Tailwind, ESLint, src/ directory).
    - Keep existing `README.md` and `spec.md` intact; do not overwrite or delete them.
    - Ensure `src/app/page.tsx` renders a minimal Trackback shell (title + placeholder copy) to confirm routing works.
  </action>
  <verify>`npm run lint`</verify>
  <done>App compiles with lint passing and renders the placeholder home page.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up default styles for a neutral baseline</name>
  <files>src/app/globals.css, src/app/page.tsx</files>
  <action>
    - Remove any default demo content or marketing boilerplate from the initial page.
    - Keep styling minimal and neutral here; visual system work happens in Plan 01-05.
  </action>
  <verify>`npm run lint`</verify>
  <done>Home page shows only the Trackback placeholder without demo boilerplate.</done>
</task>

</tasks>

<verification>
`npm run lint` passes and the home page renders without runtime errors.
</verification>

<success_criteria>
The Next.js app runs locally and provides a stable base for Phase 1 features.
</success_criteria>

<output>
After completion, create `.planning/phases/01-projects-access/01-02-SUMMARY.md`
</output>
